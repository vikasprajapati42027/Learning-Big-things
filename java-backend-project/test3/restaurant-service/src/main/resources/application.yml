server:
  port: 8091
spring:
  application:
    name: restaurant-service
  datasource:
    url: jdbc:h2:mem:restaurantdb;DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
    username: sa
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
