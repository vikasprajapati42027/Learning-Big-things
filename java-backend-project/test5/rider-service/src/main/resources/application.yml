server:
  port: 8111
spring:
  application:
    name: rider-service
  datasource:
    url: jdbc:h2:mem:riderdb;DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
    username: sa
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
