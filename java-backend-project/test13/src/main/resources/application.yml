server:
  port: 8900
spring:
  application:
    name: social-backend
  datasource:
    url: jdbc:h2:mem:social;DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
    username: sa
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
