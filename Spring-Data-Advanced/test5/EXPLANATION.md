# Test 5: Custom Repository Impl

## 🛠️ 1. Concept
When Spring Data's default methods aren't enough, you can add your own logic using a "Custom" implementation.

## 🧩 2. Requirement
You must follow the naming convention: `InterfaceName` + `Impl`.
