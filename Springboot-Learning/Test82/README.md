# Test 82: Spring Cloud Function

Serverless-friendly coding.

## Why use it?
It decouples the **Business Logic** from the **Delivery Mechanism**.
- You can call your function over HTTP (REST).
- You can trigger it via a message (Kafka).
- You can deploy it as a Serverless function (AWS Lambda, Azure Functions).

The same code works in all three places!
