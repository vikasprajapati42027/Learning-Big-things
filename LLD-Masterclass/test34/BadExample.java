public class OrderApp {
    public void createOrder() {
        // Validation logic
        if (user == null) throw new Error();
        // Save
    }
    public void updateOrder() {
        // EXACT SAME Validation logic repeated!
        if (user == null) throw new Error();
        // Update
    }
}
