# Test 10: Spring Boot Data JPA

Simplified database access.

## Concept:
Instead of writing complex SQL queries, we use JPA (Java Persistence API). You just create a Java interface, and Spring Boot automatically writes the SQL for you to Save, Find, and Delete records.
