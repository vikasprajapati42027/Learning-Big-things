/**
 * Interviewer: NoClassDefFoundError vs ClassNotFoundException?
 * Candidate: ClassNotFoundException is checked (missing in JAR). 
 * NoClassDefFoundError is an Error (found at compile time, but missing at runtime).
 */
public class Q10_NoClassDefFoundError { }
